if ("0") {
    alert( 'Hello' );
}
/*
=> Will alert be shown?
- Yes, the value "0" in  if condition statement is a string, so it's a truthy value
    truthy value remains true, so the equivalent code for this code will look like the following code
        if (true) {
            alert('Hello');
        }
*/